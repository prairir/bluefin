[arch]
image=ghcr.io/prairir/arch-toolbox:latest
init=false
nvidia=false
pull=true
root=false
replace=true
init_hooks="if [[ ! -d "${HOME}/.config/emacs" ]]; then git clone --depth 1 https://github.com/doomemacs/doomemacs ${HOME}/.config/emacs; fi"
